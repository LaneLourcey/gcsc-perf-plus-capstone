import React from 'react';
import Layout from '../components/Layout';

const IndexPage = () => (
  <Layout>
    This site is an unfinished work in progress - so much so that it's still
    missing a real homepage. Feel free to check out the audit or optimization
    section if you're feeling brave.
  </Layout>
);

export default IndexPage;
